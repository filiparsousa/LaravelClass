<nav class="navbar bg-body-tertiary">
    <div class="container-fluid">
      <span class="navbar-brand mb-0 h1" href="{{route('site.home')}}">Home</span>
      <span class="navbar-brand mb-0 h1" href="{{route('site.contacto')}}">Contacto</span>
      <span class="navbar-brand mb-0 h1" href="{{route('site.galeria')}}">Galeria</span>
      <span class="navbar-brand mb-0 h1" href="{{route('site.servicos')}}">Serviços</span>
    </div>
  </nav>

